<h1>Contacts#show</h1>
<p>Find me in app/views/contacts/show.html.erb</p>

<%= link_to "Back", :back %><br>

<div class="row">
	<div class="col s12 m6 l10">
		<div class="card">
			<div class="container">
				Name: <%= @contact.name %><br>
				Phone Number: <%= @contact.phone_number %><br>
				Email: <%= @contact.email %><br>
				Notes: <%= @contact.note_field %><br>
				Emergency Contact: <%= @contact.ice %><br>
				<div class="card-action">
					<%= link_to "Edit", edit_user_contact_path %>
					<%= link_to 'Delete', user_contact_path(current_user, @contact), data: {:confirm => 'Are you sure?'}, :method => :delete %>
				</div>
			</div>
		</div>
	</div>
</div>